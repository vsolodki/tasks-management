<p id="notice"><%= notice %></p>
<div class="d-flex justify-content-center"><h1 >Tasks</h1></div>
<% if @tasks.any? %>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Deadline at</th>
      <th>Title</th>
      <th>Note</th>
      <th>Is done</th>
      <th>Tags</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.deadline_at.strftime("%B %d, %Y %H:%M") %></td>
        <td><%= task.title %></td>
        <td><%= task.note %></td>
        <td><%= task.is_done %></td>
        <td>
          <%= task.tags.each do |tag| -%>
             <%= tag.title %><% end -%>
            </td>
        <td><%= task.category.title %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button type="button" class="btn btn-outline-primary"><%= link_to 'New Task', new_task_path %></button>
<%else %>
There is not any task, you need to create one.
<button type="button" class="btn btn-outline-primary"><%= link_to 'New Task', new_task_path %></button>
<%end %>